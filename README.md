# BLMV · B站音乐视频桌面播放器

**把音乐歌单变成 B 站视频播放列表的桌面播放器。**

你在网易云 / QQ音乐 / 酷我收藏的歌单，粘个分享链接进来，它会逐首去 B 站找最像原曲的那个视频，
用 1080P60 / 4K 的 dash 双轨流接着播；也能整个嵌进桌面壁纸层，当一块会唱歌的「动态壁纸」。
无边框 + 毛玻璃界面，颜色、透明度、模糊、阴影都能自己调。

> 名字：**BLMV** = **B**i**L**i**M**usic**V**ideo（原名 BiLiMusicVideo-desktop）

<p>
  <img alt="platform" src="https://img.shields.io/badge/platform-Windows-0078D4">
  <img alt="electron" src="https://img.shields.io/badge/Electron-42-47848F">
  <img alt="vue" src="https://img.shields.io/badge/Vue-3.5-42B883">
  <img alt="license" src="https://img.shields.io/badge/license-GPL--3.0-blue">
</p>

---

## 目录

- [功能特性](#功能特性)
- [技术栈](#技术栈)
- [快速开始](#快速开始)
- [使用说明](#使用说明)
- [配置文件位置](#配置文件位置)
- [常见问题](#常见问题)
- [实现笔记](docs/ARCHITECTURE.md)（架构 / 数据流 / 踩坑记录，改代码再看）
- [免责声明](#免责声明) · [许可证](#许可证)

## 功能特性

### 🎵 歌单：从音乐平台到 B 站视频

- **多平台歌单导入**：粘贴 **网易云音乐 / QQ音乐 / 酷我音乐** 的歌单分享链接即可，
  自动识别平台、解析出完整曲目（歌名 / 歌手 / 专辑 / 封面 / 时长）。
  纯本地解析，不依赖任何第三方聚合服务（移植自 [lx-music-desktop](https://github.com/lyswhut/lx-music-desktop) 的 `musicSdk`）。
- **多歌单管理**：想建几个歌单都行，各自记住「播放到第几首」；可新建 / 编辑名称 / 删除。
- **曲目浏览与搜索**：歌单管理弹窗右侧就是曲目表，每首带封面、歌名、歌手、专辑、时长，
  支持按 **歌名 / 歌手 / 专辑** 实时过滤；点任意一首立刻切过去播。
- **自动同步**：每个歌单可以设「手动同步 / 每次启动同步 / 定时同步」（30 分钟、1 / 3 / 6 / 12 小时、每天、每 3 天、每周），
  也能随时手动同步单个歌单或全部歌单。同步会自动补新歌、去重、清掉已下架的曲目。
- **智能匹配 + 记忆**：按歌名、歌手对 B 站搜索结果打分排序（MV / 官方 / 无损 / 高清加权，翻唱 / 伴奏 / 教学降权），
  先挑最像原曲的那个；你手动换过的视频会被记住，下次续播精确回到同一个视频。

### ▶️ 播放：能拿到多高就多高

- **dash 双轨 + MSE**：走 B 站的 dash 通道，视频与音频分开取流后用 `MediaSource` 合成，
  所以能播 **1080P60 / 4K / 8K** 以及 **Hi-Res / 杜比全景声** 音质；取不到 dash 时自动退回 720P 混流（durl）兜底。
- **清晰度 / 音质随手切**：控制栏歌曲名下面就是「清晰度」「音质」两个角标，
  点开是从当前视频 playurl 里**真正能拿到**的档位列表，选了立刻重解析。
  **只作用于当前这首歌**，下一首自动回到设置里的默认上限。
- **缓冲百分比**：切清晰度、切歌、拖进度都要重新缓冲，界面中间会给出真实进度环和百分比，不会让你以为卡死了。
- **播放模式**：列表循环 / 单曲循环 / 随机播放。
- **自动跳过死链**：播放地址带签名时效，过期会自动重新解析；视频真的挂了就重试一次再跳下一首。
- **进度记忆**：音量、播放模式、播放进度、看到哪个视频，全部记下来。

### 🖥️ 续播 / 壁纸 / 系统集成

- **自动续播**：下次打开自动回到上次的歌单、歌曲、视频和播放位置（可以只关「恢复进度」而保留「回到那首歌」）。
- **桌面壁纸模式**：一键把播放器嵌进桌面壁纸层（Windows），铺满整个桌面且不进任务栏；
  退出时按进入前的状态还原（全屏回全屏、最大化回最大化、普通窗口回原尺寸）。
- **系统媒体控件（Windows SMTC）**：歌曲名 / 歌手 / 专辑 / **封面** 会同步到系统媒体浮层与锁屏，
  键盘上的媒体键（播放暂停 / 上下曲 / 快进快退）直接可控。
- **系统托盘**：菜单里有 播放/暂停 / 上一首 / 下一首 / 循环模式 / 设置歌单 / 桌面壁纸 / 登录·退出登录 / 退出应用，
  最小化到托盘也能控制播放，点托盘图标唤回窗口。
- **账号登录**：内置 B 站登录窗口，登录后能拿到更高清晰度与会员音质；
  Cookie 持久化，重启不用重登，登录态变化会自动用新权限重新解析。
- **快捷键**：`空格` 播放暂停 · `↑ ↓` 音量 · `← →` 快进退 5 秒 · `F` 全屏 · `Esc` 退出全屏。

### 🎨 界面：能自己调的那种

- **无边框 + 毛玻璃**：自定义标题栏、控制栏、弹窗、歌单面板统一一套玻璃质感，
  弹窗点空白处即关（遮罩上有「点击空白位置关闭」提示）。
- **外观自定义**（设置 → 界面）：
  - **主题色**：玻璃底的底色，`#RRGGBB` 输入框 + 取色盘
  - **字体颜色**：所有压在视频上的文字
  - **透明度**：玻璃底有多透
  - **模糊强度 / 阴影强度**：毛玻璃的模糊半径与文字阴影大小（界面按百分比、5% 一档）
  深浅不用自己选：调完颜色会按底色亮度自动决定用黑阴影还是白阴影、弹窗控件用亮色还是暗色。
- **清晰度上限**（设置 → 播放）：自动播放时最高选到哪一档清晰度 / 音质，铺的是完整档位表。
- **缓存管理**：设置里能看到搜索缓存占用，支持「清理过期」与「全部清除」。
- **关于**：标题栏有「关于」入口，仓库地址一点就用系统浏览器打开。
- **绿色便携**：免安装版数据可以跟着程序走（见 [配置文件位置](#配置文件位置)）。

## 技术栈

| 层 | 用了什么 |
| --- | --- |
| 桌面壳 | [Electron](https://www.electronjs.org/) 42（无边框 + 透明窗口） |
| 界面 | [Vue 3](https://vuejs.org/) 3.5 + [TypeScript](https://www.typescriptlang.org/) 5.9 + [Pinia](https://pinia.vuejs.org/) 3 + [Element Plus](https://element-plus.org/) 2.14 |
| 构建 | [electron-vite](https://electron-vite.org/) 6（主进程 / preload / 渲染进程三端统一）+ [electron-builder](https://www.electron.build/) 26 |
| 播放 | `MediaSource` + 自写的 dash 分片调度（`src/utils/dashPlayer.ts`） |
| 壁纸 | [electron-as-wallpaper](https://github.com/nicehash/electron-as-wallpaper) 2.0 |
| 图标 | [iconfont](https://www.iconfont.cn/) 字体图标 |

架构、数据流、类型链路、踩坑记录都在 → **[ARCHITECTURE.md](ARCHITECTURE.md)**

## 快速开始

```bash
# 安装依赖（建议 Node 20+）
npm install

# 开发模式（electron-vite 同时起渲染进程 HMR 与 Electron）
npm run dev

# 壁纸模式开发（启动后直接嵌入桌面）
npm run dev:wall

# 类型检查（主进程 + 渲染进程）
npm run typecheck

# 构建产物到 out/
npm run build

# 打包（NSIS 安装包 + 免安装便携版，产物在 release/）
npm run dist
```

> 原生模块 `electron-as-wallpaper` 与 esbuild 的子进程在受限环境里可能被拦（`spawn EPERM`），
> 那属于沙箱限制，不是项目问题 —— 在普通终端里跑就好。

## 使用说明

1. **添加歌单**：点左上角歌单名 → 打开「歌单管理」→ 新建歌单 → 粘贴音乐平台的歌单分享链接。
   首次启动没有任何歌单时，会自动把新建框弹出来。
2. **开始播放**：在曲目表里点任意一首即可；之后底部控制栏的上一首 / 下一首就顺着这个歌单走。
3. **换视频**：控制栏第一个图标是「歌曲列表」、第二个是「视频列表」；
   视频列表里是这首歌词条 B 站搜索结果，点一条就换成它播（会记住，续播时还用它）。
4. **切清晰度 / 音质**：点歌曲名那行右侧的「清晰度」「音质」角标；
   设置里的「最高清晰度 / 最高音质」是自动播放时的上限，角标里是当前视频实际能选的档位。
5. **同步歌单**：在歌单管理里点某个歌单的同步按钮，或设置「每次启动 / 定时同步」。
6. **续播**：默认开启，下次打开回到上次的位置；不想要就在「设置 → 播放」里关。
7. **登录 B 站**：点右上角头像 → 弹出登录窗口扫码 / 登录，登录后自动用更高清晰度与音质。
8. **桌面壁纸模式**：点右上角「桌面壁纸」，再点一次（此时按钮显示「应用程序」）恢复窗口。
9. **外观**：设置 → 界面，主题色 / 字体颜色 / 透明度 / 模糊强度 / 阴影强度随你调。

## 配置文件位置

数据默认都在 `%APPDATA%/BiLiMusicVideo-desktop/`：

| 内容 | 文件 |
| --- | --- |
| 应用配置（清晰度上限、界面颜色、续播位置……） | `setting.json` |
| 歌单集合（含每首歌选定的视频） | `playlists.json` |
| 旧版单歌单（已自动迁移） | `playlist.json.migrated` |
| 搜索缓存 | `search-cache/` |

**免安装（便携）版**按下面的优先级选数据目录：

| 情况 | 用哪个目录 |
| --- | --- |
| `%APPDATA%/BiLiMusicVideo-desktop` 里**已经有数据** | 就用它（安装版 / 便携版共用一份，换版本不会「数据不见了」） |
| 没有数据，且当前是便携版 | exe 同级的 `data/`（自动创建，数据跟着程序走） |
| 没有数据，且是安装版（全新环境） | 默认的 `%APPDATA%/BiLiMusicVideo-desktop` |

想彻底干净地试用：把 `%APPDATA%/BiLiMusicVideo-desktop` 整个删掉，然后把免安装版丢进一个空文件夹里运行，
它就会在那个文件夹里建 `data/`，所有数据都在那儿。

## 常见问题

**Q：为什么清晰度只有 720P？**
B 站的 playurl 接口用 `fnval:1`（渐进式 mp4）时最高就给到 720P。本项目走的是 `fnval:16` 的 dash 通道
（音视频分开取流，再用 MSE 合成），所以能到 1080P60 / 4K —— 前提是**这个视频本身有**、且你的账号权限够。

**Q：登录了清晰度还是没变？**
登录后播放地址会按新权限重新解析（缓存里带登录态快照，识别到「缓存是未登录时取的」就会重取），
不用重启；如果当前这首歌还是旧档位，点一下角标手动选一次即可。

**Q：我在控制栏把清晰度调成 360P，为什么下一首变回 1080P 了？**
这是故意的：角标里的选择**只作用于当前这首歌**（临时试一下 / 这视频只有这个档位），
下一首会回到设置里的默认上限。想永久改就去「设置 → 播放 → 最高视频清晰度」。

**Q：进壁纸模式后为什么任务栏里还有图标？**
壁纸模式是「把窗口嵌到桌面图标层下面」，窗口本身没销毁，所以托盘和任务栏图标还在 ——
点托盘可以唤回窗口，点右上角「应用程序」回到普通窗口。

**Q：播放地址会不会过期？**
会，B 站直链带签名时效。缓存的地址每次都会检查时效，过期就重新解析；所以你看到播放器偶尔自动换一次源是正常的。

**Q：删掉缓存会怎样？**
只会让下一次搜索重新联网（慢一点点），歌单、配置、续播记录都不受影响。

## 免责声明

- 本程序为个人学习与兴趣开发，**仅供个人免费使用，请勿用于商业用途**。
- 本程序为桌面端工具，**所有数据存储在用户本地**，不涉及云端服务，不收集任何用户信息。
- B站（bilibili.com）相关接口为社区维护方案，非官方开放 API。本程序仅提供歌单解析与视频搜索的本地自动化操作，
  不破解、不修改 B 站官方客户端或网页端功能，也不提供任何音视频下载功能。
- 歌单解析功能参考 [lx-music-desktop](https://github.com/lyswhut/lx-music-desktop)（Apache-2.0）的实现思路，
  涉及的网易云 / QQ音乐 / 酷我音乐接口均为**社区逆向所得的非官方接口**，仅用于读取用户主动提供链接的歌单曲目信息
  （歌名 / 歌手 / 专辑），不涉及音源下载与破解。这些接口随时可能失效或变更。
- 若本程序涉及的任何功能侵犯了您的合法权益，请联系删除。

## 许可证

[GPL-3.0](LICENSE)
